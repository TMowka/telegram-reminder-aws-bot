service: telegram-reminder-aws-bot

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-west-1

functions:
  webhook:
    handler: handler.webhook
    environment:
      TELEGRAM_TOKEN: ${env:TELEGRAM_TOKEN}

    events:
      - http:
          path: webhook
          method: post
          cors: true
