service: telegram-reminder-aws-bot

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-west-1

functions:
  webhook:
    handler: handler.webhook

    events:
      - http:
          path: webhook
          method: post
          cors: true
